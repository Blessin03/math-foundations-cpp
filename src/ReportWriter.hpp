#pragma once

#include <fstream>
#include <string>
using namespace std;
class ReportWriter
{
private:
    /* data */
public:


    bool exportReport(const string& filename, const string& content);
};


